import { TextGenPresetPanel } from "@/components/textgen-preset/textgen-preset-panel";

export const metadata = {
  title: "文本补全预设 - SillyTavern",
};

export default function Page() {
  return <TextGenPresetPanel />;
}
